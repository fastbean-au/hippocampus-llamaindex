"""The package version.

Stamped from the git tag by the release workflow, exactly as `hippocampus-client`'s is: this
package tracks the service release it was built from, so the number is never maintained by hand.
A placeholder surviving into a build is caught by the workflow, which reads it back after stamping.
"""

__version__ = "0.0.0.dev0"
